class Solution:
    # @param s, a string
    # @return a string
    def reverseWords(self, s):
       # return ' '.join(re.compile('\s+').split(s.strip())[::-1])
        return ' '.join(re.split('\s+', s.strip())[::-1])
        #s.strip() delete leading and traing white spaces
        #both sentences are the same
